/** people per m² — a zone/link-minute at or above this is "dangerous" */
export const CRUSH = 4.0;
/** people per m² — max holding density; beyond this, spillback */
export const JAM = 5.8;
/** ₹ — scales monetary incentive in the nudge model */
export const TYPICAL_SPEND = 900;
/** share of nearby plaza population queueing for food per minute */
export const FOOD_VISIT_RATE = 0.008;
export const MAX_FOOD_STALLS_ZONE = 4;
/** extra screening lanes at one gate */
export const MAX_LANES_GATE = 8;
/** extra lanes across all gates */
export const MAX_LANES_TOTAL = 12;
