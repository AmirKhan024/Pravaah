// Test-only stub for the 'server-only' package (see vitest.config.mts). Real behavior — throwing
// when imported into a client bundle — is enforced by Next.js's own build, not by this file.
export {};
