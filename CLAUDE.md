Read SOURCE_OF_TRUTH.md fully before any task. reference/prototype.html is the source of truth for simulation behaviour.
Never let an LLM produce a number. Engine first, golden test green (`npm test`), then UI.

@AGENTS.md
